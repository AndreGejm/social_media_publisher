{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capabilities for local development.",
  "windows": ["main"],
  "permissions": [
    "default",
    "core:default",
    "dialog:default"
  ]
}

