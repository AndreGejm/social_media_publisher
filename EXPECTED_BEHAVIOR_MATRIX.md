# Expected Behavior Matrix

Status: Pass 1 complete.
Review note (2026-03-11): Rechecked after dedicated runtime error logging was added to the About workspace.

Purpose:
- Define intended behavior before fixes.
- Make assumptions explicit where UX intent is not fully settled.
- Give Pass 2 tests a contract to assert against.

Behavior assumptions to verify in Pass 2:
- A1. `About` is informational-only. Its controls may copy or refresh diagnostics, but must not alter playback queue, publish state, workspace mode, or catalog contents.
- A2. `Settings` changes local preferences only, except for explicitly destructive maintenance actions such as `Reset Library Data`.
- A3. Search and filter inputs may drive local filtering or backend-backed catalog search only. They must not trigger playback mutations, codec preparation, publish mutations, or render jobs.
- A4. Normal successful actions should be silent unless they save data, start or stop a long-running process, or resolve an error condition.
- A5. Any visible enabled control must have meaningful behavior. If no meaningful behavior exists yet, it should be disabled or removed rather than pretending to succeed.
- A6. The bottom player is a global shell element for Release Preview workspaces, including `About`. Pass 2 reproduced the current `About` omission as a defect; `Publish` remains the only mode where the player may legitimately be absent.
- A7. The `About` workspace may expose diagnostic-only values such as `Runtime Error Log`, but those fields must remain read-only and must never mutate runtime state.

## Global Shell

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| `Release Preview` / `Publish` mode tabs | Switch active app mode and persist it. When leaving a mode-only workspace, route to a valid workspace for the new mode. | Must not execute release actions, clear selections, alter queue contents, or trigger ingest/render jobs by itself. | Silent. Guidance text may update. | `activeMode`, visible workspace set, current workspace if previous one becomes invalid. | Sidebar workspace buttons, topbar subtitle, publish step shell visibility, release-selection dock visibility. | Catalog data, playback queue order, selected track, diagnostics state. |
| Sidebar workspace buttons | Switch visible workspace panel. | Must not mutate queue, filters, mode, or saved preferences unless the target workspace explicitly implies a local intent switch (for example Track vs Album QC quick links elsewhere). | Silent. | `activeWorkspace`. | Main panel body, topbar title/subtitle, conditional shells. | Catalog, queue, publish selection, playback state, render state. |
| Topbar stat pills | Navigate to the relevant workspace context (`Track QC`, `Album QC`, or `Library`). | Must not fabricate counts, apply hidden filters, or trigger ingestion/playback. | Silent. | Workspace and possibly QC intent. | Main workspace content. | Queue contents, player state, publish selection, settings. |
| Notification `Dismiss` | Remove the targeted banner only. | Must not clear unrelated banners or underlying error state outside that banner's domain. | Silent. | Specific banner state only. | Notification stack shrinks. | Workspace content, queue, player, publish data. |
| Shared/Exclusive output buttons | Request output mode change and reflect the resolved mode truthfully. If exclusive acquisition fails, recover to shared with explicit warning/error state. | Must not claim exclusive mode if acquisition failed. Must not change track, queue order, or volume by itself. | Usually silent for success. Explicit warning/error on failed exclusive acquisition. | Output mode, switching/loading state. | Output status copy, warning text, active button styling. | Queue, catalog, publish selection, current track position. |
| `Playlist` / `Queue` toggle in shared player | Toggle list mode between visible library list and session queue, using native queue visibility when available and safe fallback otherwise. | Must not raise a false playback error when fallback behavior is healthy. Must not alter queue order. | Silent on success. Error only on real failure. | `playListMode`, queue-visible flag. | List panel mode, button label, queue-mode affordances. | Track metadata, publish selection, render state. |
| `Prev` / `Next` | Move to previous or next queue item and begin playback immediately when a valid target exists. | Must not require an extra Play click after moving. Must not skip unpredictably or stop on a valid next item. | Silent. | Current track, queue index, playing state, time resets to track start. | Bottom player metadata/time, selected playback source, any track-detail time mirror for the active track. | Filters, publish selection, settings. |
| `Play` / `Pause` | Toggle playback for the active track or first queueable item. | Must not pop success toasts for normal playback. Must not change queue order. | Silent. Playback errors may surface if real playback fails. | Playing/paused state. | Button label, clock progression, waveform/progress mirrors. | Search state, publish selection, diagnostics. |
| `Stop` | Stop playback and reset current track position to the start. | Must not clear queue, unload current track, or display a success popup. | Silent. | Playing false, time resets to `0`. | Player timeline/progress. | Queue order, selected track, filters. |
| Volume slider | Send normalized volume changes and update displayed percentage. | Must not seek, change track, or trigger queue mutations. Must not spam duplicate backend writes beyond throttle policy. | Silent. | Volume scalar. | Percentage text, mute restoration cache. | Queue, track, publish state. |
| `Mute` / `Unmute` | Toggle muted state and preserve/restore the pre-mute volume. | Must not lose previous volume forever or change queue/current track. | Silent. | Muted state and effective output volume. | Button label, volume display. | Queue, filters, publish state, catalog. |
| Shared player seek | Seek within the current track. | Must not change queue order or selected track. | Silent. | Current time. | Timeline thumb, elapsed time, QC mirrors if same source is shown elsewhere. | Search, settings, publish state. |

## Library Workspace

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| Library collapses (`Library Ingest`, `Library overview`, `Quick actions`) | Expand or collapse only the targeted section and persist that preference. | Must not remove the section permanently or create layout gaps that imply broken content. | Silent. | Section expanded/collapsed state. | Panel visibility and layout density. | Catalog data, queue, player, publish selection. |
| `Scan Folders` / `Import Files` tabs | Switch ingest sidebar subpanel. | Must not run ingest automatically on tab switch. | Silent. | Active ingest tab. | Visible ingest form. | Library roots, queue, player. |
| `Library root path` input | Accept/edit folder path text only. | Must not add a root, browse folders, or start scan by typing alone. | Silent. Validation only on submit/action. | Input value. | Text field contents. | Queue, player, publish selection, current catalog. |
| `Browse...` | Open native folder picker and populate input if a folder is chosen. | Must not add the root automatically unless the user confirms with `Add Folder`. | Silent unless picker fails. | Input value, browse pending state. | Button loading label, root input text. | Library root list, queue, player. |
| `Add Folder` | Persist the current path as a library root. | Must not scan automatically unless explicitly designed later and documented. Must not add duplicates silently if duplicates are disallowed. | Small status/notice allowed because this changes persisted state. | Library roots list, mutating state. | Root row appears or updates. | Queue order, playback state, publish selection. |
| `Refresh Folders` | Reload library roots and their scan/job status. | Must not add/remove roots or trigger new scans. | Silent. | Loading state. | Root list and status text. | Queue, player, publish selection. |
| `Scan Folder` | Start or resume a scan job for that specific saved root. | Must not scan a different root, clear current catalog view, or interrupt playback unnecessarily. | Status text expected because this starts long-running work. | Ingest job state, loading flags, catalog contents as results land. | Root row status, counts, possibly autoplay hook when designed. | Publish selection except stale-pruning on real data removal. |
| `Cancel Scan` | Request cancellation for that root's active ingest job and show truthful terminal state. | Must not leave job in a fake completed state or restore stale queue/catalog state. | Status text expected. | Ingest job status. | Root row status and job controls. | Player transport truth, unrelated roots, settings. |
| `Remove Folder` | Remove the root and prune imported catalog state tied to that root. | Must not delete local media files on disk. Must not leave stale queue/favorite/publish-selection references. | Notice or warning acceptable because destructive local state changed. | Library roots, catalog rows, queue/favorite/publish references. | Counts, visible rows, queue/favorites if pruned. | Preferences, unrelated roots, publish workflow form fields already loaded. |
| `Import file paths` textarea | Accept newline/comma-separated file paths only. | Must not start import by typing alone. | Silent. | Textarea contents. | Textarea value. | Queue, player, publish selection. |
| `Import Files` | Import listed files into the catalog and surface any failures explicitly. | Must not pretend success when files fail. Must not create scan roots unless that behavior is explicitly documented elsewhere. | Status/notice expected. | Importing state, catalog contents, failure list. | Counts, import failures panel, list results, optional autoplay only if that explicit workflow is active. | Existing roots list, publish selection, settings. |
| `Open Track QC` / `Open Album QC` / `Open Publish Workflow` | Navigate to the named workspace or mode only. | Must not run ingest, mutate queue, or save metadata as a side effect. | Silent. | Workspace and possibly QC intent or mode. | Main panel and sidebar state. | Catalog, queue contents, player state unless target workspace intentionally reuses it. |

## Quality Control and Playlists

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| `Track QC` / `Album QC` tabs | Switch QC intent between track-level and album-level review. | Must not clear selection, queue, or current playback source. | Silent. | `qualityControlMode`. | Main QC panel body. | Catalog data, publish selection, settings. |
| `Search tracks` | Filter/search the catalog by title, artist, album, or path. Backend-backed search is acceptable if it remains search-only. | Must not trigger ffmpeg, render jobs, queue mutations, playback restarts, codec-session resets, or publish mutations. | Silent. No success popups. | Search query, visible list page. | Track rows, empty state, load-more eligibility. | Player state, queue order, publish selection, settings. |
| `Track sort` / `Track grouping` | Reorder or regroup the visible list deterministically. | Must not change track metadata or queue order unless queue mode intentionally mirrors the session queue. | Silent. | Sort key / grouping mode. | Row ordering/group headers. | Playback state, publish selection, ingest jobs. |
| `Refresh List` | Reload catalog rows using the current search/filter context. | Must not reset search text, favorites filter, or QC mode unexpectedly. | Silent. | Loading flags, refreshed page data. | Track list contents, loading label. | Queue order, player, publish selection. |
| `Library` / `Queue` tabs | Toggle list source between catalog/library view and current session queue view. | Must not destroy queue order when entering queue mode. Must not clear selection unless rows disappear legitimately. | Silent. | List mode. | Row source, queue controls, empty-state messaging. | Playback source, publish selection, settings. |
| `All Tracks` / `Favorites Only` | Toggle favorites-only filtering. | Must not mutate favorite membership, queue order, or current playback. | Silent. | Favorites filter state. | Visible rows, empty state, counts. | Queue contents, publish selection, settings. |
| `Album QC View` | Switch to album QC intent. | Must not alter search text, queue order, or playback state. | Silent. | QC intent. | Album panel visibility. | Publish selection, settings. |
| `Shuffle` | Randomize the current session queue order deterministically enough for a session-level queue action. | Must not drop or duplicate queue items. | Silent. | Queue ordering. | Queue rows, queue index mapping. | Track metadata, publish selection, settings. |
| `Clear Queue` | Remove the manual queue lock so playback follows visible list order again. | Must not clear the catalog or current search/filter state. | Silent. | Session queue lock/use state. | Queue badge/pill, row source. | Favorites, publish selection, settings. |
| Row selection checkboxes | Add or remove items from current batch selection. | Must not play audio or reorder queue by themselves. | Silent. | Batch selection set. | Batch action toolbar visibility. | Queue order, player state, publish selection. |
| `Play Selection` | Replace the current session queue with selected visible rows and start the first selected row immediately. | Must not start on a different row or preserve stale queue items ahead of the new selection. | Silent. | Queue contents, queue index, playback state. | Shared player metadata, queue rows, selection state as designed. | Publish selection, settings, catalog metadata. |
| `Add Selection to Queue` | Append selected rows to the end of the current queue in visible order. | Must not start playback automatically unless explicitly documented later. | Silent. | Queue contents. | Queue rows/counts, queue-related banners if used. | Current playback state, publish selection, settings. |
| `Play Selection Next` | Insert selected rows immediately after the current queue item in visible order. | Must not reorder unrelated queue segments. | Silent. | Queue contents/order. | Queue rows and upcoming order. | Current track playback until next boundary, publish selection. |
| `Clear Selection` | Clear current batch selection only. | Must not clear queue or favorites. | Silent. | Batch selection state. | Batch action toolbar hides. | Queue, playback, publish selection. |
| Track row main button | Select/focus the row in track detail or make it the active list row. | Must not start playback just because the row was selected. | Silent. | Selected track. | Track detail panel, contextual actions, metadata preview. | Queue order, publish selection, settings. |
| Row menu button and context menu | Open contextual actions for the correct row. | Must not target a different row or close without action feedback when an item is clicked. | Silent. | Context-menu open state. | Menu visibility. | Queue, player, publish selection until an action is chosen. |
| Context action `Play Now` | Start playback for that row immediately and update queue source/order consistently. | Must not require a second click on global Play. | Silent. | Current track, queue source/order, playing state. | Shared player, selected-row state, queue order if session queue changes. | Publish selection, settings. |
| Context action `Add to Queue` | Append row to queue. | Must not interrupt current playback. | Silent. | Queue contents. | Queue count/rows. | Current track, publish selection, settings. |
| Context action `Play Next` | Insert row after current queue item. | Must not start immediately unless current item ends or user presses Next. | Silent. | Queue contents/order. | Queue preview/order. | Current playback, publish selection. |
| Context action `Add Favorite` / `Remove Favorite` | Toggle favorite membership for that row. | Must not mutate queue or current playback. | Silent. | Favorite set. | Favorite markers, favorites-only results. | Publish selection, settings. |
| Context action `Show in Track QC` | Move to Track QC and focus the chosen row. | Must not start playback or mutate queue. | Silent. | Workspace/QC intent/selected track. | Main panel changes to Track QC detail view. | Queue, publish selection, settings. |
| Context action `Add to Selection` | Add that row to batch selection. | Must not play or reorder. | Silent. | Batch selection set. | Batch action toolbar. | Queue, publish selection. |
| Queue-only context actions `Remove from Queue`, `Move Up`, `Move Down` | Mutate queue order or membership deterministically. Keyboard and drag-drop behavior should agree with these semantics. | Must not corrupt selection, duplicate tracks, or desync UI from actual queue state. | Silent. | Queue order/membership. | Queue rows, queue index, upcoming order. | Catalog metadata, publish selection, settings. |
| `Load more tracks` | Fetch the next page of search results without losing current query/order intent. | Must not duplicate already loaded rows or reset list to page 1. | Silent. | Pagination cursor/page state. | Additional rows, button loading label. | Player, queue, publish selection, settings. |
| Album group row | Select that album group for detail view. | Must not auto-play album on selection alone. | Silent. | Selected album. | Album detail panel. | Queue, player, publish selection. |
| `Play Album` | Queue album tracks in album order and start playback from track 1. | Must not skip tracks or start from the wrong track. | Silent. | Queue contents/index, playback state. | Shared player, queue rows. | Publish selection, settings. |
| `Add Album to Queue` | Append album tracks in album order. | Must not interrupt current playback. | Silent. | Queue contents. | Queue rows/counts. | Current track, publish selection. |
| Album `Show in Track QC` | Switch to Track QC and focus the first album track. | Must not alter queue or playback by itself. | Silent. | QC intent, selected track. | Track detail panel. | Queue, publish selection, settings. |
| Album track main button | Open that track in Track QC. | Must not auto-play by selection alone. | Silent. | Selected track, QC intent. | Track QC detail view. | Queue, publish selection. |

## Track Detail, QC Player, Codec Preview, Batch Export

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| Track Detail `Play Now`, `Add to Queue`, `Play Next`, `Favorite` / `Unfavorite` | Mirror the corresponding queue/favorite semantics for the currently selected track. | Must not diverge from row-context actions or duplicate action rows elsewhere inconsistently. | Silent. | Queue/favorite/playback state. | Shared player, row markers, queue order. | Publish selection, settings. |
| `Edit Metadata` | Enter single-panel edit mode for the selected track. | Must not persist changes before save. | Silent. | Edit mode flag. | Editable fields, save/cancel/reset actions. | Queue, playback, publish selection. |
| `Save Metadata` | Persist validated metadata to the local catalog and survive remount/reload. | Must not claim success if persistence failed or data did not change. | Explicit success or error status is appropriate because data is saved. | Saved track detail, edit dirty state, notice/error state. | Detail values, notices/banners. | Queue order, player state, publish selection, settings. |
| `Reset Fields` | Restore editor fields from the last saved metadata while staying in edit mode. | Must not leave edit mode or save implicitly. | Silent. | Editor field values. | Input values and dirty indicator. | Queue, player, publish selection. |
| `Cancel Edit` | Exit edit mode and discard unsaved field edits. | Must not save silently. | Silent. | Edit mode false, editor reset. | Read-only metadata view. | Queue, player, publish selection. |
| Metadata fields (`Visibility`, `License`, `Downloadable`, `Tags`) | Update only the local edit state until save; validate on save boundary. | Must not mutate queue, playback, publish workflow, or hidden backend jobs while typing/selecting. | Silent during editing; errors only on invalid save. | Local editor state. | Dirty markers, prepared tag count, validation errors. | Queue, player, publish selection, render/ffmpeg. |
| `Prepare for Release...` | Create/load a draft spec from the selected track and switch into Publish workflow with the draft available in the selection dock and form. | Must not execute the release or clear listening queue. | Notice/status acceptable because it bridges workflows and persists a draft artifact. | Publish selection list, draft prefill, mode/workspace. | Publish step shell, dock rows, embedded Publisher Ops inputs. | Listening queue contents, playback state, settings. |
| QC waveform seek bar / playback slider / `-5%` / `+5%` | Seek the active QC track precisely and update the shared transport mirror when that track is the live source. | Must not change queue order, selected track, or publish selection. | Silent. | Playback position. | Waveform playhead, time labels, shared player time. | Queue contents, metadata, settings. |
| `Codec profile A` / `Codec profile B` | Choose the comparison profiles for preview generation. | Must not pretend to switch audio variants if no valid preview session exists. | Silent while valid; explicit error only on real preview-prep failure. | Preview profile ids, session readiness. | Session status copy, enabled/disabled state of variant buttons. | Queue order, publish selection, track metadata. |
| `Enable Blind-X mode` | Turn Blind-X behavior on or off for the current preview session. | Must not reveal identity prematurely when enabled. | Silent. | Blind-X enabled state. | Session status copy, Blind-X button enabled state. | Queue, publish selection, settings. |
| Variant buttons `Bypass`, `Codec A`, `Codec B`, `Blind-X` | Switch the actual playback source used for preview comparison. | Must not be UI-only toggles disconnected from the actual source path. | Silent. Real failures should be explicit. | Active preview variant and active playback source. | Session status, shared player metadata/source path if surfaced elsewhere. | Queue order, publish selection, metadata. |
| `Reveal` | Reveal which real profile Blind-X mapped to, without changing the historical mapping for the current session. | Must not remap Blind-X to a new profile or alter queue state. | Silent or small status text. | Blind-X revealed state. | Session status text. | Queue, player index, publish selection. |
| Batch export profile checkboxes and fields | Update only the pending export request. | Must not start export until `Start Batch Export` is pressed. | Silent. | Pending export request state. | Selected profiles, field values. | Queue, playback, publish selection. |
| `Start Batch Export` | Start a background export job for the selected profiles and show truthful job status, including explicit failure outcomes. | Must not report `completed` when encoding did not happen. | Status text expected because this is long-running work. | Export job state/submission state. | Status copy, disabled state, failure/success messages. | Queue, current playback, publish workflow, settings. |

## Video Workspace

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| `Save Project`, `Load Project`, `Save Preset`, `Load Preset` | Persist or restore video workspace state exactly as labeled. | Must not change library/publish state or silently drop invalid references without a warning. | Status text expected. | Video project/preset persistence state. | Media, text, output, preview, recent directories, status line. | Queue, playback queue, catalog, publish selection. |
| `Dismiss Status` | Clear only the current video persistence status message. | Must not reset project state. | Silent. | Status-message visibility. | Status line/button visibility. | Video settings and all non-video systems. |
| Image/audio file pickers, native browse buttons, and drop zones | Import the correct media type into the correct slot, show metadata, and reject unsupported types clearly. | Must not swap slots, accept unsupported types silently, or start rendering automatically. | Status/alert on failure only. | Video project media assets. | Metadata cards, readiness copy, preview assets. | Queue, publish workflow, settings. |
| Media `Clear` buttons | Remove only that asset from the project. | Must not clear the other asset or unrelated settings. | Silent. | Image or audio asset state. | Media summary card, preview readiness. | Non-video workspaces and transport queue. |
| Visual controls (fit mode, overlay enable/position/opacity/intensity/smoothing/color) | Update the visual preview deterministically and only for video composition state. | Must not change source media files or kick off renders just because the preview changed. | Silent. Validation/error only if analysis fails. | Overlay and preview settings. | Preview frame, overlay bars, status text. | Queue, publish selection, catalog. |
| Text controls (`Enable text layer`, preset, title, artist, size, color, `Reset Text`) | Update text overlay state predictably, keeping title/artist editable even when a preset hides artist rendering. | Must not mutate output paths or render state. | Silent. Warning only for invalid text settings. | Text overlay settings. | Preview text overlay, counts, warnings. | Queue, catalog, publish workflow. |
| Output controls (`Output preset`, directory, recent directories, filename, overwrite policy) | Update pending render output settings and derived output preview path. | Must not render automatically or mutate saved project unless explicitly saved. | Silent. Validation errors are appropriate. | Output settings and recent directory state. | File preview text, preset summary, validation message. | Playback queue, publish selection, catalog. |
| Preview `Play` / `Pause`, `Restart`, position slider | Control local video preview playback only. | Must not alter the app-wide shared player queue or current music QC playback state. | Silent. | Local preview playback state and position. | Preview progress labels, play button label. | Shared player, library queue, publish selection. |
| `Build Render Request` | Build deterministic render request JSON and show it for inspection. | Must not start a render job. | Silent or status line update. | Request object state. | Request summary and JSON block. | Shared player, queue, publish selection. |
| `Refresh Diagnostics` | Re-read render-environment readiness. | Must not mutate project inputs or start rendering. | Silent unless diagnostics fail. | Diagnostics state. | Diagnostics status/error copy. | Queue, publish selection, catalog. |
| `Render MP4` | Start rendering only when media and output preflight are valid; update progress and final summary truthfully. | Must not start when diagnostics/preflight fail. Must not report success when render failed. | Progress and status text expected. | Render phase, progress, result state. | Status lines, preflight issues, error/success summary, open-folder button. | Shared player queue, publish workflow, settings. |
| `Cancel Render` | Request cancellation of the active render and surface truthful terminal state. | Must not leave the UI in a fake completed state. | Status text expected. | Render phase/result. | Button enabled state, runtime status. | Queue, catalog, publish selection. |
| `Reset` in render panel | Reset render-controller state without clearing the project inputs. | Must not delete selected media, text, or output settings unless explicitly documented later. | Silent. | Render controller transient state. | Status lines, request/result summaries. | Media selection, preview settings, non-video systems. |
| `Open Output Folder` | Open the folder for a successful render result only. | Must not claim success if no output exists. | Silent or small status line. | Open-folder status message only. | Success summary/status line. | Project data, shared player, publish workflow. |
| Alert dismiss controls | Clear only the shown import issue/status alert. | Must not drop the underlying project state. | Silent. | Alert visibility only. | Alert disappears. | All other systems. |

## Settings Workspace

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| Theme and palette selects | Update the local shell theme and palette only. | Must not change workspace, queue, or publish workflow state. | Silent. | Theme preference and palette preference. | CSS variables, theme data attributes, palette list. | Catalog, queue, publish selection, playback truth. |
| `Compact density`, `Show full local file paths`, `On file drop, also add each file's parent folder as a scan root` | Update only the named local preference. | Must not immediately mutate library data beyond the next relevant user action. | Silent. | Local preference flags. | Layout density, path display, future drop behavior. | Existing queue, current playback, publish workflow. |
| Shortcut capture inputs | Capture and persist the new binding for that action. | Must not fire the bound action while the user is editing the field. | Silent. Conflict warning allowed. | Shortcut binding map. | Displayed binding text, conflict warning. | Queue, playback, catalog, publish data until user later triggers the shortcut. |
| Shortcut-row `Clear` | Remove only that binding. | Must not reset other shortcut rows. | Silent. | One binding becomes unassigned. | Binding text and conflict state. | All runtime state. |
| `Reset Shortcuts` | Restore the default shortcut set. | Must not clear unrelated settings. | Silent. | Entire shortcut binding map. | Binding text and conflict state. | Queue, playback, catalog, publish data. |
| `Clear Notice` / `Clear Error Banner` | Clear only the named banner type. | Must not clear unrelated errors/notices or mutate underlying domain state beyond the banner itself. | Silent. | Banner visibility only. | Notification stack. | Queue, playback, publish selection, catalog contents. |
| `Reset Library Data` | Clear persisted local roots, catalog rows, ingest jobs, queue/favorites/session selections, and related local state. | Must not delete source media files from disk or leave stale in-memory rows behind. | Explicit status/notice expected because destructive local state changed. | Catalog/session state reset. | Counts, list rows, queue, favorites, publish selection, root list. | Theme, shortcut preferences, video workspace settings, about diagnostics. |

## About Workspace

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| `Runtime Error Log` (read-only field) | Show the resolved runtime error log path, or a truthful unavailable-state message when the path cannot be resolved in the current runtime. | Must not be clickable, editable, or trigger logging work by merely rendering. | Silent. | Informational display value only. | System information card and copied diagnostics text. | Queue, playback, publish state, settings, catalog. |
| `Copy System Info` | Copy current version/platform/runtime diagnostics text, including the runtime error log path shown in About, to clipboard. | Must not navigate, switch modes, or mutate workflow state. | Small copy status is acceptable. | Copy-feedback text only. | Feedback line under resources. | Queue, playback, publish state, settings, catalog. |
| `Refresh Diagnostics` | Re-fetch diagnostics for informational/support use. | Must not trigger publish/render/playback actions or change mode. | Silent or light status only. | Diagnostics loading/ready/error state. | System information values and helper text. | Queue, playback, publish selection, catalog, settings. |

## Publish Workflow Shell and Embedded Publisher Ops

| Control | Should happen | Should not happen | Feedback | Allowed state changes | Related UI updates | Must remain unaffected |
| --- | --- | --- | --- | --- | --- | --- |
| Publish step tabs (`New Release`, `Plan / Preview`, `Execute`, `Report / History`) | Switch visible publish screen and stay in sync with the embedded Publisher Ops screen. | Must not execute release actions or clear prepared selections just because the tab changed. | Silent. | Active publish step/screen. | Main publish panel body. | Listening queue, playback state, settings. |
| Release Selection `Clear Selection` | Clear prepared publish candidates from the dock. | Must not clear the listening queue or catalog. | Silent or small notice. | Publish-selection list. | Dock rows and counts. | Listening queue, current playback, settings. |
| Release Selection `Open Track QC` | Return to Release Preview Track QC to prepare more tracks. | Must not discard already prepared drafts. | Silent. | Mode/workspace switch only. | Dock hides, Track QC shows. | Existing release selections, queue, settings. |
| Release Selection row `Load ... into Publish workflow` | Load that draft's spec/media into the form and make it the active draft. | Must not execute the release. | Silent or small status. | Active draft selection and form inputs. | Dock active-row styling, Publisher Ops inputs. | Listening queue, settings. |
| Release Selection row `Remove ... from release selection` | Remove that prepared draft from the dock list. | Must not delete catalog tracks or source media. | Silent. | Selection list only. | Dock rows/counts. | Listening queue, playback, settings. |
| `Spec File Path` / `Media File Path` inputs | Accept or edit local paths only. | Must not plan/execute by typing alone. | Silent. Validation on action boundaries. | Form values. | Input text, validation errors. | History, release-selection dock, listening queue. |
| `Environment` select | Change the plan/execute environment label. | Must not execute or refresh history automatically. | Silent. | Form value. | Environment summary chip. | Queue, catalog, settings. |
| `Mock connector` checkbox | Include or exclude the mock platform in the pending request. | Must not plan automatically. | Silent. | Selected platform list. | Selected platforms summary. | Queue, catalog, settings. |
| `Load Spec` | Parse and validate the spec and advance the review state when valid. | Must not execute or create external side effects. | Explicit success/error text is appropriate. | Loaded spec state, active screen may move to preview. | Spec summary, status text. | Listening queue, release-selection list, settings. |
| `Plan / Preview` | Build a release plan deterministically from current inputs and selected platforms. | Must not publish/commit externally in preview mode. | Explicit status text is appropriate. | Plan result, active screen, history refresh side effects. | Planned actions list, execute gate hint, summaries. | Listening queue, settings. |
| `Execute` | Run execution only when a valid plan/release id exists and surface truthful result/history/report updates. | Must not be enabled before prerequisites are satisfied. Must not silently succeed on failure. | Explicit status text is appropriate. | Execute result, history rows, report state, active screen. | Execute summary, history/report panels. | Listening queue, settings. |
| `Refresh History` | Reload history rows from local storage/runtime. | Must not execute a release. | Silent or status text. | History rows/loading state. | History list and summary text. | Listening queue, settings. |
| `Open Release Report` | Load the report for the currently selected history row. | Must not execute or change selection implicitly. | Silent or status text. | Report state/loading state. | Report summary and actions list. | Listening queue, settings. |
| `Resume Release` | Resume execution for the selected release using persisted state. | Must not resume a different release than the selected row. | Explicit status text is appropriate. | Execute/result/history/report state. | Same areas as `Execute`. | Listening queue, settings. |
| History radio rows | Change the selected history target for report/resume actions. | Must not open a report or resume automatically on selection alone. | Silent. | Selected release id. | Enabled/disabled state of report/resume buttons. | Listening queue, queue state, settings. |

